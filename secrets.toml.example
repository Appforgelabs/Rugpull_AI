# Copy to .streamlit/secrets.toml for LOCAL runs (gitignored).
# On Streamlit Community Cloud, paste this same content into
# Settings -> Secrets instead — do NOT commit the real file.
FMP_API_KEY = "your_fmp_key_here"
